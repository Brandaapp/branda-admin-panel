/**
 * Use this when hitting endoints, we're going to have to switch it out
 * with the actual server url somewhere down the line and we do not
 * want localhost:3001 to be hardcoded everwhere in our program
 */
const SERVER_URL = "http://localhost:3001";

module.exports = SERVER_URL;